/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/visualizador-opensarc",
  output: "export",
  images: {
    loader: "akamai",
    path: "",
  },
};

export default nextConfig;
